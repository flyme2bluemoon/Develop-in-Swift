let bestLanguage = "Swift"
let worstLanguage = "Java"
let guess = "Python"

if (guess == bestLanguage) {
    print("You're right! \(bestLanguage) is the best language!")
} else if (guess == worstLanguage) {
    print("\(worstLanguage) is a shameful excuse of a programming language...")
} else {
    print("Although \(guess) is not the best language, it is miles ahead of \(worstLanguage).")
}